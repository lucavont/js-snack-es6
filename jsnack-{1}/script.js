// JSNACKS 1A

let newObject = {
    nome: "palla",
    peso: 10,
}
console.log(newObject.peso)
    // JSNACKS 1B

newObject.peso = parseInt(prompt('Inserisci un nuovo peso:'))

console.log(newObject.peso)